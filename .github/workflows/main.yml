on:
  workflow_dispatch:

jobs:
  my_job:
    name: test echo
    runs-on: ubuntu-18.04
    steps:
      - name: checkout repo
        uses: actions/checkout@v2.7.0
      - name: pwd output
        run: pwd
      - name: ls output
        run: ls -a
      - name: Done Outputs
        run: echo done with testing
      - name: Trying Change Directory
        run: cd qsb/lua/tools
      - name: pwd output
        run: pwd
      - name: ls output
        run: ls -a
